# 公有化说明

本分支是从内部集成基线生成的无历史公有化快照，提交历史不包含内部仓库早期的作者、实例或运行记录。

## 已移除内容

- 云平台控制脚本、登录/浏览器自动化和实例配置；
- `v1-result/` 运行证据、会话快照、容器/卷信息和模型发布记录；
- 嵌套的私有 `nanoproof` 工作树及其 Git 元数据；
- 模型权重、训练输出、日志和构建缓存。

## 运行约定

模型路径、容器镜像、服务地址和凭据均由部署环境通过参数或环境变量提供。请勿把 token、Cookie、SSH 私钥、实例标识、个人路径或运行快照提交到仓库。

提交前运行：

```bash
bash tools/scan-secrets.sh
```
